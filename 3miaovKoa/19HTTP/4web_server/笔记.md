```
注意：头信息的设置需要早于返回数据。



首先需要把静态资源和动态资源进行区分。

1、静态资源：
    （1）即进行多次请求之后，返回的数据不会发生变化的文件。比如：html文件，js文件，image文件和css文件等
    （2）目前将静态数据放在static文件夹下，封装成一个方法

2、动态资源：
    即我们常说的接口数据。如这里的getUser



引入的插件
    // 通过判断文件类型返回content-type对应的值
    (1)const mime = require('mime')

    (2)vue.min.js


node.js作为中间层解决跨域问题。
    使用http模块，创建接口'/getbaidu'

    跨域问题只是浏览器同域下的限制，但是对于服务端来说，并不存在这种限制。如现在我想访问京东的首页：www.jd.com，肯定请求不了，这就涉及到了跨域问题。那现在我在node.js中设计一个接口来获取jd的页面，这样就能解决跨域问题了。详情见：server1.js + baidu.html

```