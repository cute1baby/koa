```
1、什么是Symbol?
    Symbol: 表示独一无二的值，他是js中第七种数据类型

2、Symbol函数不能使用new，否则会报错，原因在于Symbol是一个原始类型的值，不是对象。

3、Symbol函数接收一个字符串作为参数，表示对Symbol的描述，主要是为了在控制台显示，或者转为字符串的时候，比较容易区分。


```



```
Symbol数据类型转换


```